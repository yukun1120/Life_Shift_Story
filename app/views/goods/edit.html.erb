<div class="home">
  
  <%= render "shared/side"%>

  <div class="home-main">

    <%= render "shared/header"%>

    <div class="new-m-main">
      <div class="good-contents row">
        <div class="good-container">
          <div class="good-container-back">
            <%= link_to "戻る", goods_path %>
          </div>
          <h3>今日あったいいこと</h3>
          <%= form_with(model: @good, local: true) do |form| %>
            <%= form.text_field :good_thing, placeholder: "今日あったいいこと", class: "value-content"%>
            <%= form.submit "完了" %>
          <% end %>
        </div>
      </div>
    </div>

    <%= render "shared/footer"%>
    
  </div>

</div>